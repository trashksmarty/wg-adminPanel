spring:
  datasource:
    # Подключение к уже запущенному внешнему PostgreSQL-контейнеру.
    url: jdbc:postgresql://${DB_HOST:192.168.51.171}:${DB_PORT:5432}/${DB_NAME:wg_admin}
    username: ${DB_USER:wg_admin_user}
    password: ${DB_PASSWORD:HgfPsxccWalOr32}
  jpa:
    hibernate:
      # Для MVP разрешаем автосоздание/обновление схемы.
      ddl-auto: update
    open-in-view: false

server:
  # Порт backend-приложения.
  port: ${APP_PORT:8080}

easy-wireguard:
  # URL удалённого API easy-wireguard.
  base-url: ${EW_API_BASE_URL:http://localhost:51821}
  # Токен доступа к API.
  token: ${EW_API_TOKEN:change-me}
